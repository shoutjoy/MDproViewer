{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local main window event subscriptions for native file drops",
  "windows": ["main"],
  "permissions": ["core:default"]
}
